global_costmap:
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 3.0       # Update frequently
  publish_frequency: 2.0
  static_map: false
  rolling_window: true
  width: 100.0                # Large map
  height: 100.0
  resolution: 0.2
  transform_tolerance: 1.0
  
  plugins:
    - {name: obstacle_layer, type: "costmap_2d::ObstacleLayer"}
    - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

# GLOBAL COSTMAP - For long-range planning

# GLOBAL COSTMAP - LARGER SIZE

# global_costmap:
#   global_frame: odom
#   robot_base_frame: base_link
  
#   update_frequency: 2.0
#   publish_frequency: 1.0
  
#   static_map: false
#   rolling_window: true
  
#   # INCREASED SIZE - 100m x 100m instead of 50m
#   width: 1000.0
#   height: 1000.0
#   resolution: 0.2          # Coarser resolution (20cm) for large area
  
#   transform_tolerance: 1.0  # INCREASED - more tolerance
  
#   # Origin always centered on robot
#   origin_x: 0.0
#   origin_y: 0.0
